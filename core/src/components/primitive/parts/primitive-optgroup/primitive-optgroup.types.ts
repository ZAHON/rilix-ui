import type { PropsOf, Component } from '@builder.io/qwik';

export interface PrimitiveOptgroupProps extends PropsOf<'optgroup'> {
  /**
   * The component that this component should render as.
   */
  as?: Component;
}
