export interface ContainsParams {
  /**
   * The parent element to check against.
   */
  container: HTMLElement;

  /**
   * The child element whose containment is being verified.
   */
  element: HTMLElement;
}
